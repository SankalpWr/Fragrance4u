# fragrance4u

This repository contains the source for the "fragrance4u" perfume-picker frontend app (Vite + React + TypeScript).

Quick start

1. Install dependencies: `npm install`
2. Run locally: `npm run dev`

See `package.json` for scripts and further details.
